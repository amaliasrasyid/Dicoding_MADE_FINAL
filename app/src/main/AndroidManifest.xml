<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="amalia.dev.dicodingmade">

    <!-- Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <permission android:name="amalia.dev.dicodingmade.READ_DATABASE" android:protectionLevel="normal"/>
    <permission android:name="amalia.dev.dicodingmade.WRITE_DATABASE" android:protectionLevel="normal"/>

    <application
        android:name=".repository.realm.RealmApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <provider
            android:name=".provider.FavProvider"
            android:authorities="amalia.dev.dicodingmade"
            android:exported="true"
            android:readPermission="amalia.dev.dicodingmade.READ_DATABASE"
            android:writePermission="amalia.dev.dicodingmade.WRITE_DATABASE"/>

        <activity
            android:name=".view.tvshow.TvShowDetailActivity"
            android:parentActivityName=".view.MainActivity" />
        <activity
            android:name=".view.movie.MovieDetailActivity"
            android:parentActivityName=".view.MainActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>